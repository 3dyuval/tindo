# websocket with nitro + vite -monorepo template

**Prerequisites**

- node
- pnp

**How to Run**

# Runs both client and server
`pnpm dev`
Copy
# Run the client only
`pnpm dev:client`
Copy
# Run the server only
`pnpm dev:server`
